{
	"name": "@local/plugin-stripe-cart",
	"version": "0.1.0",
	"type": "module",
	"private": true,
	"description": "Stripe checkout + cart plugin for EmDash. Server-authoritative pricing from the `products` collection. Trusted (Node-only) plugin — uses the Stripe SDK directly.",
	"exports": {
		".": "./src/index.ts",
		"./sandbox": "./src/sandbox-entry.ts"
	},
	"dependencies": {
		"stripe": "^17.0.0"
	},
	"peerDependencies": {
		"emdash": "^0.9.0"
	}
}
